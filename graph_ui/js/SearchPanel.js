var React = require('react');

var SearchPanel = React.createClass({
  render: function() {
    return (
      <div>
        <h1>Search Panel</h1>
      </div>
    );
  }
});

module.exports = SearchPanel;
